import { Typography } from '@mui/material';
import type { NextPage } from 'next';

const Profile: NextPage = () => {
  return (
    <Typography variant="h1" margin={10} textAlign="center">
      Profile page
    </Typography>
  );
};

export default Profile;
